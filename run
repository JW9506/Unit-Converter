#!/bin/bash

args=
for y in "$@"; do
  if [ "$y" == "$1" ]; then continue; fi
  args="$args $y"
done

file=$(basename $1 .java)
dir=$(dirname $1)
if [ "$dir" != "" ]; then
  dir="$dir/"
fi
javac "$dir$file.java" && java "$dir$file" $args
